#ifndef OSAFORK_COLORS_H
#define OSAFORK_COLORS_H

#include "color.h"

// this file should be deleted in future
namespace colors {
    const Color BLACK = Color(0x000000);
    const Color WHITE = Color(0xFFFFFF);
    const Color YELLOW = Color(0xFFFF00);
    const Color AQUA = Color(0x00FFFF);

    const Color TUBE_GREEN = Color(0, 230, 0);
}

#endif //OSAFORK_COLORS_H
